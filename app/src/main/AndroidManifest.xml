<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marglov2">

    <application
        android:allowBackup="false"
        android:fullBackupOnly="false"
        android:icon="@drawable/logofinal9"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logofinal8"
        android:supportsRtl="true"
        android:theme="@style/Theme.marglo">

        <activity
            android:name="com.example.marglov2.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.marglo.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>


</manifest>